/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ["https://pixabay.com","cdn.pixabay.com"],
    },
}

module.exports = nextConfig
